# SheetForge v7 — Python Dependencies
# Pipeline: medianBlur → adaptiveThreshold → MORPH_OPEN → Canny → approxPolyDP → DXF + PDF

# Core Computer Vision
# Use headless variant for server/Render deployment (no GUI/display dependencies)
opencv-contrib-python-headless==4.11.0.86
numpy>=2.4.4

# DXF Export
ezdxf==1.4.4

# PDF Export
reportlab>=4.2.0

# EXIF/DPI reading from uploaded images
Pillow==12.2.0
