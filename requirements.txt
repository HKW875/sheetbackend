# SheetForge v4 — Python Dependencies

# Core Computer Vision & Image Processing
# IMPORTANT: install exactly ONE of the three opencv variants.
# opencv-python-headless is correct for server/cloud deployments (no GUI/display needed).
# opencv-python and opencv-contrib-python must NOT be installed alongside it —
# all three ship the same cv2.so and overwrite each other, breaking the import.
opencv-contrib-python-headless==4.10.0.84

# NumPy — pin to 1.x; numpy 2.x broke the binary ABI for many cv2/torch builds
numpy>=1.24.0,<2.0.0

Pillow==10.4.0
scikit-image>=0.21.0
scipy>=1.11.0

# DXF & CAD
ezdxf==1.4.4

# OCR
pytesseract==0.3.13

# AI & Vision Models
google-generativeai>=0.8.0
ultralytics>=8.2.0
segment-anything>=1.0
easyocr>=1.7.1

# Export & Visualization
matplotlib>=3.8.0
svgwrite>=1.4.0
reportlab>=4.2.0

# Utils
google-genai>=1.0.0
scikit-learn>=1.3.0
torch>=2.0.0
torchvision>=0.15.0
# torchaudio is optional — only needed if you use audio features
# torchaudio>=2.0.0


flask==3.0.3
flask-cors==4.0.1
requests==2.31.0
cloudinary==1.40.0
pymongo==4.7.1
python-dotenv==1.0.1
imutils==0.5.4

